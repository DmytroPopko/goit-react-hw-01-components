import styled from '@emotion/styled';

export const Th = styled.th`
  font-weight: bold;
  padding: 5px;
  background: ${props => props.theme.colors.bgTransactionTitle};
  border: 1px solid #dddddd;
`;
